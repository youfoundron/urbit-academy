:: Q8. Lists and Lests II

:: Start with your answer to the previous question
:: and modify it so it returns [~winter ~diglet ~ponnys ~]

:: Hint: i.starlist grabs the head of the list, and t.starlist grabs the tail of the list.

:: Previous code
=/  starlist
  `(lest @p)`[~sonnet ~winter ~diglet ~ponnys ~]
i.starlist

:: Answer
=/  starlist
  `(lest @p)`[~sonnet ~winter ~diglet ~ponnys ~]
t.starlist
